<Window x:Class="lab_4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Система Массового Обслуживания (СМО)" 
        Height="700" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель параметров -->
        <GroupBox Grid.Row="0" Header="Параметры системы" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Margin="5">
                    <TextBlock Text="Среднее время между заявками:"/>
                    <TextBox x:Name="txtMeanGenerationTime" Text="2.0" Margin="0,5,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Margin="5">
                    <TextBlock Text="Время обслуживания:"/>
                    <TextBox x:Name="txtServiceTime" Text="1.5" Margin="0,5,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2" Margin="5">
                    <TextBlock Text="Размер очереди:"/>
                    <TextBox x:Name="txtQueueSize" Text="5" Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Управление -->
        <GroupBox Grid.Row="1" Header="Управление" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="btnStepByStep" Content="Пошаговый метод" 
                        Click="BtnStepByStep_Click" Height="30" Margin="5" Width="120"/>
                <Button x:Name="btnEventBased" Content="Событийный метод" 
                        Click="BtnEventBased_Click" Height="30" Margin="5" Width="120"/>
                <Button x:Name="btnFindOptimal" Content="Найти оптимальную очередь" 
                        Click="BtnFindOptimal_Click" Height="30" Margin="5" Width="150"/>
                <Button x:Name="btnClear" Content="Очистить" 
                        Click="BtnClear_Click" Height="30" Margin="5" Width="80"/>
            </StackPanel>
        </GroupBox>

        <!-- Результаты -->
        <GroupBox Grid.Row="2" Header="Результаты и лог событий" Margin="0,0,0,10">
            <TextBox x:Name="txtLog" IsReadOnly="True" VerticalScrollBarVisibility="Auto" 
                   HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="10"/>
        </GroupBox>

        <!-- Статус -->
        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock x:Name="txtStatus" Text="Готов к работе"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>